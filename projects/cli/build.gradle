plugins {
	id 'application-conventions'
}

dependencies {
	implementation project(':projects:common')

	implementation 'org.slf4j:slf4j-api'
	runtimeOnly 'ch.qos.logback:logback-classic'
}

application {
	mainClass.set('fr.damnardev.template.gradle.cli.Startup')
}
